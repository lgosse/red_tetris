import React from "react";

import {
  FullSizeContainer,
  FlexContainer,
} from '../../components/helpers/Common';

const Party = () => {
  return (
    <FullSizeContainer>
      <FlexContainer>
        PARTY
      </FlexContainer>
    </FullSizeContainer>
  );
};

export default Party;
